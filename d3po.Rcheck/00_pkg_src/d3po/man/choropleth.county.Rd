% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/choropleth.county.R
\name{choropleth.county}
\alias{choropleth.county}
\title{County level choropleth}
\usage{
choropleth.county(
  df,
  id.column = "id",
  value.column = "value",
  legend.title = "",
  legend.text.size = 20,
  scale.text.size = 16,
  color.domain = NULL,
  num.legend.ticks = 5,
  color.scheme = c("Blues", names(d3po::color.schemes)),
  width = NULL,
  height = NULL,
  viewer = c("internal", "external", "browser")
)
}
\arguments{
\item{df}{data.frame containing value data by county.}

\item{id.column}{Name of column containing identifiers for states/counties. Defaults to "id". All values in this column must match \code{d3po::us.counties$id}.}

\item{value.column}{Name of column containing values by county. Defaults to "value".}

\item{legend.title}{Title of legend, e.g., units. Defaults to "".}

\item{legend.text.size}{Size of text (in points) for legend title. Defaults to 20.}

\item{scale.text.size}{Size of text (in points) for the scale values. Defaults to 16.}

\item{color.domain}{Range of values for the color scale. Defaults to \code{c(min(df[,value.column]), max(df[,value.column]))}. Length greater than two results in a multi-point gradient.}

\item{num.legend.ticks}{Number of breaks in legend scale. Defaults to 5.}

\item{color.scheme}{Color scheme to use in visualization. See \link[d3po]{color.schemes} for more details.}

\item{width}{Desired width for output widget.}

\item{height}{Desired height for output widget.}

\item{viewer}{"internal" to use the RStudio internal viewer pane for output; "external" to display in an external RStudio window; "browser" to display in an external browser.}
}
\value{
A d3 object as returned by \link[r2d3]{r2d3}.
}
\description{
Creates choropleth at the U.S. county level.
}
\details{
Utilizes a script similar to \url{https://observablehq.com/@d3/choropleth} adapted to work with r2d3.
}
\examples{
\dontrun{
data(unemployment.county)

choropleth.county(unemployment.county,
                 id.column = "id",
                 value.column = "rate",
                 legend.title = "Unemployment rate (\%)")
}

}
